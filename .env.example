# Port to expose the API on
# API_PORT=8000

# Set to 1 to include score breakdown in search responses
SHODOUKAN_DEBUG=0

# Comma-separated list of allowed frontend origins
# Development: * (allow all)
# Production:  https://your-app.onrender.com
CORS_ORIGINS=*
