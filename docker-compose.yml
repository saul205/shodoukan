services:
  api:
    build:
      context: .
      dockerfile: packages/shodoukan-api/Dockerfile
    ports:
      - "${API_PORT:-8000}:8000"
    env_file:
      - .env.dev
    restart: unless-stopped
