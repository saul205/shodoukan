services:
  api:
    build:
      context: .
      dockerfile: packages/shodoukan-api/Dockerfile
    ports:
      - "${API_PORT:-8000}:8000"
    environment:
      - SHODOUKAN_DEBUG=${SHODOUKAN_DEBUG:-0}
    restart: unless-stopped
